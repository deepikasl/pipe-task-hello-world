{
  "name": "jira-create-issue",
  "version": "0.0.1",
  "description": "Create a Jira issue",
  "main": "src/index.js",
  "scripts": {
    "dist": "mkdir ./dist && cp ./index.js ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "1.1.3",
    "jfrog-pipelines-tasks": "0.0.5"
  }
}
